You are a technical writer. Your task is to generate a `quickstart.md` file for the `conduit` Python library based on the provided project code.

## Instructions

1.  Start with an "Installation" section, providing the `pip` or `uv` command.
2.  Add a "Setup" section, showing how to set the required API key (e.g., `OPENAI_API_KEY`) as an environment variable.
3.  Create a "Basic Usage" section. This must contain one complete, minimal, copy-pasteable code block that demonstrates the *absolute simplest way* to get a result from the library (e.g., a single chat completion).

## Style Guidelines

  - **Do not** use the first person ("I", "we").
  - **Do not** use icons or emojis.
  - **Do not** use lofty, breathless, or promotional language. The tone should be direct, factual, and accessible.
  - **Do not** add sections for licenses, contributions, or any meta-badges (like PyPI version or build status). Focus only on the user's task.

## Project Code

<project_code>
{{project_code}}
</project_code>
